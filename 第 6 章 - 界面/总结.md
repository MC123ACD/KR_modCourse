# (一) 逻辑
## 一、界面加载逻辑
### [一] 加载 `main` 进行初始化
1. 游戏运行
2. 加载 love 2d 引擎
3. 加载 `main.lua` 模块
4. 加载完毕调用 `love.load()`
5. 进入主循环，开始按帧调用 `love.update(dt)`、`love.draw()` 等函数

# (二) 游戏界面 `game_gui`
`game_gui` 为进入关卡后的界面，能力、英雄肖像等

# (三) 地图界面 `screen_map`
`screen_map` 为地图界面，英雄殿堂、成就等