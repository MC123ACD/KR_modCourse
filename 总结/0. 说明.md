总结用于存放一些表格、函数说明等

# 名词说明
### [一] 插入、更新、移除系统
插入、更新、移除系统特指 `all/systems` 内的所有对应函数（`on_insert`、`on_remove`、`on_update`）

一个系统中的函数返回假将会阻止执行后续函数
通常用于插入实体时进行判断，判断未通过返回假阻止创建实体，判断通过正常创建实体

# 逻辑
主要总结逻辑，细节请自行查看代码