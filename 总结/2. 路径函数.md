### [一] 返回路径宽度
```lua
P:path_width(路径: int, 子路径: int, 节点: int) -> 宽度: int
```
- **作用**：返回路径宽度

### [二] 返回节点坐标
```lua
P:node_pos(路径: int, 子路径: int, 节点: int, 是否返回引用?: bool) -> 节点坐标: vec2
```
- **作用**：返回节点坐标

### [三] 返回偏移后节点坐标
```lua
P:node_offset_pos(偏移量: int, 路径: int, 子路径: int, 节点: int) -> 节点坐标: vec2
```
- **作用**：返回偏移后节点坐标

### [四] 返回起点的节点
```lua
P:get_start_node(路径: int) -> 节点: int
```
- **作用**：返回起点的节点

### [五] 返回终点的节点
```lua
P:get_end_node(路径: int) -> 节点: int
```
- **作用**：返回终点的节点

### [六] 返回防守点的节点
```lua
P:get_defend_point_node(路径: int) -> 节点: int
```
- **作用**：返回防守点的节点

### [七] 返回节点到起点距离
```lua
P:nodes_from_start(路径: int, 子路径: int, 节点: int) -> 节点距离: int
```
- **作用**：返回节点到起点节点距离

### [八] 返回节点到终点距离
```lua
P:nodes_to_goal(路径: int, 子路径: int, 节点: int) -> 节点距离: int
```
- **作用**：返回节点到终点节点距离

### [九] 返回节点到防守点距离
```lua
P:nodes_to_defend_point(路径: int, 子路径: int, 节点: int) -> 节点距离: int
```
- **作用**：返回节点到防守点节点距离

### [十] 返回可见终点的节点
```lua
P:get_visible_end_node(路径: int) -> 节点: int
```
- **作用**：返回可见终点的节点

### [十一] 返回可见起点的节点
```lua
P:get_visible_start_node(路径: int) -> 节点: int
```
- **作用**：返回可见起点的节点

### [十二] 判断一定距离是否存在节点
```lua
P:point_within_distance(x: num, y: num, 距离: num) -> 是否存在: bool
```
- **作用**：判断一定距离是否存在节点

### [十三] 返回附近节点
```lua
P:nearest_nodes(x: num, y: num, 路径表: table{ 路径: int ... }, 子路径表?: table{ 子路径: int ... }, 是否仅有效节点: bool, 标签: int, 过滤函数?: func, 步长: int) -> 节点表: table{ table{ 路径: int, 子路径: int, 节点: int, 距离: num } ... }
```
- **作用**：从给定的路径与子路径返回一个根据距离排序的表：表包含所有节点的所在路径、子路径、节点、距离
- 子路径表默认值 { 1 }

### [十四] 返回地形类型
```lua
P:path_terrain_types(路径: int) -> 地形标志: int
```
- **作用**：返回地形类型

### [十五] 返回地形属性
```lua
P:path_terrain_props(路径: int) -> 地形标志: int
```
- **作用**：返回地形属性

### [十六] 判断路径是否激活
```lua
P:is_path_active(路径: int) -> 是否激活: bool
```
- **作用**：判断路径是否激活

### [十七] 激活路径
```lua
P:activate_path(路径: int)
```
- **作用**：激活路径

### [十八] 关闭路径
```lua
P:deactivate_path(路径: int)
```
- **作用**：关闭路径

### [十九] 增加无效节点范围
```lua
P:add_invalid_range(路径: int, 从: int, 到: int, 标签: int)
```
- **作用**：增加无效节点范围

### [二十] 移除无效节点范围
```lua
P:remove_invalid_range(路径: int, 从: int, 到: int)
```
- **作用**：移除无效节点范围

### [二十一] 判断节点是否有效
```lua
P:is_node_valid(路径: int, 节点: int, 标签: int) -> 是否有效: bool
```
- **作用**：判断节点是否有效

### [二十二] 返回所有有效节点
```lua
P:get_valid_nodes(路径: int, 标签: int) -> 有效节点表: table{ 节点: int ... }
```
- **作用**：返回所有有效节点

### [二十三] 返回第一个找到的有效节点
```lua
P:find_valid_node(路径: int, 开始节点: int, 步长: int, 标签: int) -> 节点: int
```
- **作用**：返回第一个找到的有效节点

### [二十四] 判断附近有没有有效节点
```lua
P:valid_node_nearby(x: num, y: num, 路径宽度乘数: num, 标签: int) -> 是否存在: bool
```
- **作用**：判断附近有没有有效节点

### [二十五] 返回随机节点信息
```lua
P:get_random_position(边距: int | table{ 上边距: int, 下边距: int }, 地形标签: int, 标签: int, 边距是否从防御点开始计算: bool) -> 节点坐标: vec2, 路径: int, 子路径: int, 节点: int
```
- **作用**：返回随机节点的坐标、节点所在路径、子路径、节点

### [二十六] 返回连接的下一路径
```lua
P:get_next_pi(路径: int) -> 下一路径: int
```
- **作用**：返回连接的下一路径

### [二十七] 返回所有连接的路径
```lua
P:get_connected_paths(路径: int) -> 连接路径表: table{ 路径: int ... }
```
- **作用**：返回所有连接的路径

### [二十八] 返回所有有效节点
```lua
P:get_all_valid_pos(x: num, y: num, 最小距离: num, 最大距离: num, 地形标签: int, 过滤函数: func, 标签: int, 子路径表: table{ 子路径: int }) -> 有效节点表: table{ 节点: int ... }
```
- **作用**：返回指定范围所有有效节点
